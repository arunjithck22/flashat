export enum Gender {
    Male = "Male",
    Female = "Female",
    PreferNotToSay = "Prefer Not to Say",
    None = "",
  }

  export enum ProfileCheckpoints {
    HomePage = 0,
    PasswordSetup = 1,
    ProfileSetup = 2,
    UsernameSetup = 3,
    SuperStarSetup = 5,
  }
  
  export enum Devicetype{
    WEB="web"
  }